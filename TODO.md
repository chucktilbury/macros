# TODO list

* Flag a syntax error when a macro is defined with parameters, but expanded without them.
* Flag a syntax error when a macro is defined with no parameters, but is expanded with them.
* Make directive names not case sensitive.
* Add syntax error if the command line option does not match the type. Specifically numbers.

# NOTES
* Look at decorating macro names for parameter overloading.